- flash.each do |name, msg|
  %div{:class => "alert fade in alert-#{name == :notice ? "success" : "error"}"}
    %a#closebtn.close{:href => "#", "data-dismiss" => "alert"} ×
    = content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String)
